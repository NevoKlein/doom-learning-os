# Tests

Use this directory for automated checks as the Learning OS is refactored.

Minimum checks to add before a major migration:
- HTML loads without console errors.
- curriculum/resource JSON parses.
- all resource URLs are present and well-formed.
- localStorage migration preserves existing progress and notes.
- search/filter/progress/export behavior remains functional.

